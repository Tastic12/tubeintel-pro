/** Latest uploads checked per tracked channel (YouTube playlist page max = 50). */
export const COMPETITOR_VIDEOS_PER_CHANNEL = 50;

/** Skip YouTube sync if channel was synced within this window. */
export const COMPETITOR_VIDEO_SYNC_INTERVAL_MS = 60 * 60 * 1000;
